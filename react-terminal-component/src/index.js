import ReactTerminal from 'react-terminal-component/src/ReactTerminal';
import ReactTerminalStateless from 'react-terminal-component/src/ReactTerminalStateless';
import ReactThemes from 'react-terminal-component/src/themes';
import ReactOutputRenderers from 'react-terminal-component/src/output';

export default ReactTerminal;

export {
  ReactTerminal, ReactTerminalStateless, ReactThemes, ReactOutputRenderers
};
