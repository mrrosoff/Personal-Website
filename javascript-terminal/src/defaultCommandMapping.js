import cat from "./commands/cat";
import cd from "./commands/cd";
import clear from "./commands/clear";
import cp from "./commands/cp";

const defaultCommandMapping = { cat, cd, clear, cp }

export default defaultCommandMapping;
